# -*- coding: cp1250 -*-
import random
import math

pi = 3.14

def funkce(x):
    return math.sin(x)

def monte(xmin, xmax, ymin, ymax, N):
    ''' Bude se generovat N náhodných bodů [x,y] ve čtverečku definovanym 
    souřadnicema xmin, xmax, ymin, ymax (např. x: (0, pi), y: (0, 1)) a
    za úkol máš vrátit na základě počtu úspěšnejch a neúspěšnejch pokusů, jak 
    velký je místo pod funkcí funkce(x). '''
    
    # here comes the kote
    
    return obsah
    
print("Funkce sin(x) na intervalu (0,pi) X (0,1) uzavira pod krivkou: ") 
print(monte(0, pi, 0, 1, 100));

# bonus - ukázat jak se hodnota blíží realitě s rostoucím počtem pokusů :)
# na to se budou hodit grafy, s tím můžeš počkat
